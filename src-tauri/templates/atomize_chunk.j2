You are a software architect. Split the following implementation plan into logical implementation sections.

Each section should represent a cohesive area of work (e.g., "Database schema", "API endpoints", "Frontend components").

Return a JSON array of section objects. Each section must have:
- "title": short section name
- "content": the relevant portion of the plan for that section

Return ONLY valid JSON, no markdown code fences, no explanation.

Example output:
[
  {"title": "Database schema", "content": "..."},
  {"title": "API endpoints", "content": "..."}
]

---

{{ condensed_plan }}
