[package]
name = "loopforge-app-core"
version = "0.1.0"
edition = "2021"
description = "Application core services for LoopForge backend orchestration"

[dependencies]
app-services = { path = "../app-services" }

[lints]
workspace = true
